#ifndef FCT_H_INCLUDED
#define FCT_H_INCLUDED

#include<string>

void etoile(std::string a);
bool inc(std::string a,char letter);
std::string star(std::string a);
std::string apparaitre(std::string sol,char letter,std::string a);
bool win(std::string a, std::string sol);
bool let(int i, std::string a,char letter);

#endif // FCT_H_INCLUDED
